# MCP TempMail v7.0 config
HOST=127.0.0.1
PORT=19876          # random high port – change if needed (avoids 8000 clashes)
LOG_LEVEL=info

# Set to true ONLY if you want real tempamail.com (requires playwright)
PLAYWRIGHT_ENABLED=false

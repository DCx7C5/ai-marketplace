---
apply: off
instructions:
---
Read `.plan/rules.md` and `.plan/development-workflow.md` before doing any work. Then inspect `.plan/session.db` for active and ready todos, and read the nearest local planning markdown for the target area. The hook loader builds the current tool inventory on every sessionStart, so use the injected tool list instead of a hardcoded set.

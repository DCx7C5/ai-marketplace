---
apply: off
instructions:
---
When `/resume-or-start-working` is submitted, load `.github/workflows/resume-or-start-working-chain.md` so the `resume-or-start-working batch` is injected before runtime lookup and the worker then checks for its own runtime entry or starts a new claim.
